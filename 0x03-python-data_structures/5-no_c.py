#!/usr/bin/python3
#3-print_reversed_list_integer.py


def no_c(my_string):
    """Ommits c in strings printed"""
    new_string = ""
    for char in (my_string):
        if char != 'c' and char !=:
        (new_string) += char
    return (new_string)
